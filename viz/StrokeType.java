package viz;

/**
 * An enumeration of the line stroke types available in XAAL.
 * Currently 'dotted' doesn't work in JHAVE.
 * @author Eric
 *
 */
public enum StrokeType {
	solid,
	dashed,
	dotted
}
